/**
 * Application — CLI orchestration and runner.
 *
 * The `Runner` class lives in `runner.ts` but is not re-exported here
 * to avoid coupling the public API surface to application-layer details.
 * Consumers should import Runner directly from "./runner".
 */
